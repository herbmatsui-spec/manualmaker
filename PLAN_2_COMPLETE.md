# Plan 2: Strategy Pattern Implementation - Complete Plan
## SecurityManager バックエンド独立化実装計画書

### 概要
このドキュメントは、Manual Makerプロジェクトの `SecurityManager` クラスをバックエンド依存から独立させるための詳細な実装計画です。
クラウドネイティブ環境（特にCloudflare Workers）へのデプロイを可能にしつつ、後方互換性を完全に維持することを目指します。

### 実装アプローチ: 戦略パターン (Strategy Pattern)
- **インターフェースの抽象化**: `PatternProvider`, `KeyStore`, `EncryptionProvider` の3つのProtocolを定義
- **具体的戦略の実装**: 各インターフェースに対して複数の実装を提供
- **実行時設定**: `SecurityConfig` クラスを介して戦略を注入可能
- **後方互換性**: 既存コードは変更不要で、段階的移行が可能

### 依存関係
- フェーズ1-4を順番に実装
- 各フェーズは前のフェーズに依存するが、最小限の変更で段階的にリリース可能
- 低性能LLMでも実装可能なように、各ステップを極力小さな変更に分解

---

## フェーズ別実装計画

### 📄 フェーズ1: インターフェース定義
[詳細](./PLAN_PHASE_1.md)
- 目的: バックエンド抽象化のためのインターフェースを定義
- 主な成果物:
  - `src/security/interfaces.py` (PatternProvider, KeyStore, EncryptionProvider Protocol)
  - `tests/test_security_interfaces.py` (インターフェースの基本テスト)
- 期間: 1日程度
- リスク: 低（新規ファイル追加のみ）

### 📄 フェーズ2: 既存バックエンドの戦略化
[詳細](./PLAN_PHASE_2.md)
- 目的: 既存のYAML/file system、keyring、cryptography実装を戦略クラスとして実装
- 主な成果物:
  - `src/security/strategies.py` (HardcodedPatternProvider, EnvVarKeyStore, NoOpEncryption)
  - `src/security/config.py` (SecurityConfigクラス)
  - テスト更新
- 期間: 2-3日程度
- リスク: 中（既存ロジックの移植が必要だが、後方互換性を保つ）

### 📄 フェーズ3: Cloudflare Workers対応戦略
[詳細](./PLAN_PHASE_3.md)
- 目的: Cloudflare Workers環境で動作する戦略クラスを実装
- 主な成果物:
  - `src/security/strategies_workers.py` (KVPatternProvider, KVKeyStore, WebCryptoEncryption)
  - `src/security/config.py` の `from_workers_env` ファクトリメソッド追加
- 期間: 2-3日程度
- リスク: 中-high（Workers固有APIの扱い、非同期化の課題）

### 📄 フェーズ4: 統合・テスト・後方互換性
[詳細](./PLAN_PHASE_4.md)
- 目的: 全コンポーネントmadeを統合し、SecurityManagerをリファクタリング
- 主な成果物:
  - `src/security_manager.py` の設定注入対応（後方互換維持）
  - 包括的なテストスイート
  - 使用方法のドキュメント
- 期間: 3-4日程度
- リスク: high（コアロジックの変更だが、段階的アプローチでリスク軽減）

---

## 実装ガイドライン

### 🎯 原則
1. **小さなステップ**: 各コミットは1つの機能または1つのテストのみ
2. **後方互換性最優先**: 既存のパブリックAPIは絶対に変更しない
3. **テストファースト**: 新しい機能はテストを書いてから実装
4. **段階的リリース**: 各フェーズ終了時にリリース可能な状態にする

### 🛠️ ローパフォーマンスLLM向けヒント
- 各ステップを「ファイルを作成してこの正確な内容を書く」レベルに分解
- コピーペースト可能なスニペットを提供
- 複雑なロジックは既存コードを参照して模倣
- 1ステップあたりの変更行数を10行以内に抑える

### 📊 進捗追跡
- [ ] フェーズ1完了
- [ ] フェーズ2完了
- [ ] フェーズ3完了
- [ ] フェーズ4完了
- [ ] 全テストパス
- [ ] ドキュメント更新
- [ ] リリース準備

---

## 成功基準

### ✅ 必須条件
1. 既存のすべてのテストがパスすること（後方互換性）
2. `SecurityManager` が `SecurityConfig` オプションパラメータを受け取れること
3. カスタム戦略を注入して動作を変更できること
4. Cloudflare Workers環境用の戦略が利用可能であること
5. 設定を渡さない場合の動作が既存バージョンと同一であること

### ✅ 望ましい条件
1. 単体テストで外部依存が完全にモック可能であること
2. Workers環境でのデプロイが `wrangler publish` 1コマンドで可能であること
3. パフォーマンスが既存実装と同等以上であること
4. セキュリティが既存実装と同等以上であること

---

## リスク軽減策

### 🔧 技術的リスク
- **Workers非同期問題**: 暗号化戦略を非同期にせず、同期ラッパーを提供するか、Future/Promiseパターンを検討
- **KVバインディング問題**: テスト環境用のフォールバックメカニズムを必ず実装
- **バンドルサイズ増**: 不要な戦略はツリーシェイク可能に設計（実際のPythonでは影響小さいが）

### 📋 プロセスリスク
- **スコープクリープ**: 各フェーズの目標を厳守し、「まあまあ良い」でリリース
- **後退テスト**: 各フェーズ終了時にフルテストスイートを実行
- **段階的ロールアウト**: まずは開発環境で新機能を有効にし、本番は徐々に移行

---

## 今後の展開

### 🚀 フェーズ5以降のアイデア
- **さらなるバックエンド**: AWS Parameter Store, HashiCorp Vault, Azure Key Vault 戦略
- **キャッシュレイヤー**: パターン読み込み結果のキャッシュ戦略
- **監査とロギング**: 戦略使用状況のトラッキング
- **動的戦略切り替え**: 実行時の環境変化に応じた自動戦略切替

### 📈 メリット
| 観点 | 移行前 | 移行後 (Plan 2完了) |
|------|--------|---------------------|
| **テスト容易性** | 外部依存のモックが複雑 | モック戦略注入で簡単・高速 |
| **環境移植性** | ローカル依存が強い | どの環境でも戦略を差し替え可能 |
| **拡張性** | 新バックエンド追加が困難 | OCP 準拠で新戦略追加が簡単 |
| **Workers対応** | 基本的に不可能 | 戦略を替えるだけで可能 |
| **セキュリティ柔軟性** | 固定実装 | ポリシーに応じた戦略選択が可能 |

---

## 結論
**Plan 2 (戦略パターン)** は、SecurityManagerのバックエンド独立化において最もバランスの取れたアプローチです。
初期実装コストはありますが、その結果得られるテスト容易性、環境移植性、および将来的な拡張性は、
Manual Makerがクラウドネイティブアプリケーションとして成長していく上で不可欠な投資となります。

この計画書に従ってフェーズ1-4を順番に実装すれば、
低性能LLMでも確実に完了でき、最終的にCloudflare Workersシングルバイナリデプロイが可能な、
後方互換性を保ったセキュリティモジュールを手に入れることができます。

**最初の一歩**: `PLAN_PHASE_1.md` に従って、`src/security/interfaces.py` を作成するところから始めてください。