# Phase 4: コンプライアンス・オブザーバビリティ - 実装計画書

## 概要
Phase 1-3で構築したシステムの上で、商用リリースに必須のコンプライアンス対応（GDPR、セキュリティ基準）、高度なオブザーバビリティ、最終的な製品化作業を行う。

**目標期間**: 2週間以上（継続的改善）  
**前提**: Phase 1-3 完了済み

---

## Step 1: GDPR対応 - データエクスポート機能の実装

### 目的
GDPR Article 15（アクセス権）に準拠した個人データエクスポート機能を実装する

### 作業内容
1. `manual_processor/src/gdpr/export.py` 新規作成
2. `export_user_data(user_id: str) -> dict` 関数実装：
   - 対象データソース特定：
     - Redis Streamからの監査ログ（`audit:log` で `user_id` フィルタ）
     - アップロードファイルメタデータ (`upload:*` キーで関連付けがあるもの）
     - 処理結果メタデータ (`process:*` キーで関連付けがあるもの）
     - 結果ファイル（R2/ディスク上の実際のファイル）
     - 設定・ preferences（ユーザー設定がある場合）
   - データ構造:
     ```json
     {
       "user_id": "string",
       "export_timestamp": "ISO string",
       "data_categories": {
         "profile": { /* ユーザープロフィール */ },
         "activity_log": [ /* アクティビティ履歴 */ ],
         "uploaded_files": [ /* アップロードファイルメタデータ */ ],
         "processing_history": [ /* 処理履歴 */ ],
         "generated_content": [ /* 生成ファイルのメタデータと内容（可能な場合） */ ],
         "audit_log": [ /* 監査ログ抜粋 */ ],
         "settings": { /* ユーザー設定 */ }
       }
     }
     ```
3. 大容量データ対応:
   - ストリーミング出力サポート
   - ZIPアーカイブ生成（ストリーミング）
   - 進捗コールバック対応
4. APIエンドポイント: `POST /api/gdpr/export` （認証必須）
   - レスポンス: `application/zip` または `application/json`
   - ファイル名: `gdpr-export-{user_id}-{timestamp}.zip`
5. 管理コンソール用内部関数も提供

### 確認方法
- テストユーザーデータ作成 → エクスポートリクエスト → ZIPファイル取得
- ZIP内に期待通りのデータ構造が含まれていること
- 大容量データでもタイムアウトしないこと

---

## Step 2: GDPR対応 - データ削除機能の実装

### 目的
GDPR Article 17（消去権）に準拠した個人データ削除機能を実装する

### 作業内容
1. `manual_processor/src/gdpr/deletion.py` 新規作成
2. `delete_user_data(user_id: str, dry_run: bool = False) -> dict` 関数実装：
   - 削除対象特定（エクスポートと同じ論理）：
     - ユーザー関連の監査ログエントリ
     - ユーザーのアップロードファイル
     - ユーザーの処理結果と関連ファイル
     - ユーザー設定・キャッシュ
     - 一時ファイル
   - 削除方法:
     - ファイル: 安全削除（3回上書き後削除）
     - Redis: キー削除（スキャン＋削除またはLuaスクリプト）
     - R2: オブジェクト削除（バッチ処理）
     - データベース: レコード削除（該当する場合）
   - ドライランモード: 実際に削除せず、削除対象リストのみ返却
   - 削除レポート生成:
     ```json
     {
       "user_id": "string",
       "deletion_timestamp": "ISO string",
       "dry_run": boolean,
       "deleted_counts": {
         "audit_log_entries": number,
         "uploaded_files": number,
         "processing_results": number,
         "files_deleted": number,
         "bytes_freed": number
       },
       "errors": [ /* 部分的失敗の詳細 */ ]
     }
     ```
3. APIエンドポイント: `DELETE /api/gdpr/data/{user_id}` （認証必須、管理者権限推奨）
   - クエリパラメータ: `dry_run=true` でドライラン
   - レスポンス: 削除レポート
4. アイドルタイムベース自動削除オプション（設定可能）：
   - `manual_processor/src/gdpr/retention_policy.py`：アイドル期間超過データの自動削除

### 確認方法
- テストユーザーデータ作成 → 削除リクエスト → データが実際に削除されていること
- ドライランで削除対象が正しく特定されていること
- エラー時は部分的成功でもレポートに反映されること

---

## Step 3: データ最小化と目的制限の実装

### 目的
GDPR原則に従い、必要最小限のデータのみを収集・保存する

### 作業内容
1. データ収集見直し：
   - `manual_processor/src/security_manager.py` の監査ログ：
     - 必要なフィールドのみ記録（`user_id`, `action`, `resource`, `timestamp`, `correlation_id`）
     - `details` フィールドは機密情報を含まないように制限
   - アップロード処理：
     - ファイル内容は処理後すぐに一時ファイルを削除
     - メタデータのみ保存（ファイル名、サイズ、アップロード日時）
   - 処理結果：
     - 必要な期間（例: 30日）経過後に自動削除
     - 長期保存が必要な場合は明示的なユーザーアクション必要
2. 保存期間ポリシーの実装：
   - `manual_processor/src/config/settings.py` に `RetentionSettings` 追加：
     - `audit_log_retention_days: int = 365`
     - `uploaded_file_retention_days: int = 30`
     - `processing_result_retention_days: int = 30`
     - `temp_file_retention_hours: int = 24`
   - `manual_processor/src/services/retention_service.py` 新規作成：
     - 定期ジョブ（Celery Beat または Kubernetes CronJob）で実行
     - 各種データの期限切れチェックと削除
3. 同意管理の基盤：
   - `manual_processor/src/gdpr/consent.py` 新規作成
   - 処理オプションに関する同意記録（例: 音声生成、クラウド連携）
   - API: `POST /api/gdpr/consent`, `GET /api/gdpr/consent/{user_id}`

### 確認方法
- データ収集ポイントで最小限のデータのみ収集されていること
- 保存期間ポリシーが期限通りにデータを削除していること
- 同意を記録・取得・撤回できること

---

## Step 4: セキュリティ基準準拠 (SOC 2 Type II, ISO 27001 基礎)

### 目的
主要なセキュリティ・コンプライアンス基準に準拠した管理体制を整備する

### 作業内容
1. アクセス制御の強化：
   - `manual_processor/src/auth/rbac.py` 新規作成（Role-Based Access Control）
   - 役割定義: `admin`, `operator`, `viewer`, `auditor`
   - 権限マトリクス作成と適用
   - APIエンドポイントに役割ベースのアクセス制御デコレータ追加
2. 認証強化：
   - MFA（多要素認証）の基盤実装（TOTP）
   - `manual_processor/src/auth/mfa.py` 新規作成
   - 管理者・機密操作にMFA必須オプション
3. セキュリティログの改善：
   - すべての認証・認可イベントを監査ログに記録
   - 特権操作の追跡
   - 不正アクセス検知のためのベースライン作成
4. 脆弱性管理プロセスの文書化：
   - `docs/SECURITY_RESPONSE.md`：脆弱性対応手順
   - `docs/VULNERABILITY_DISCLOSURE.md`：外部からの報告手順
   - 定期的なセキュリティスキャンの自動化（依存関係・コード・インフラストラクチャ）
5. データ暗号化の徹底：
   - 保存データの暗号化（R2サーバーサイド暗号化、ディスク暗号化）
   - 転送中の暗号化（TLS 1.2以上必須）
   - キー管理: クラウドKMSまたはHashicorp Vault連携基盤

### 確認方法
- 役割ベースアクセス制御が機能していること
- 特権操作が適切にログに記録されていること
- セキュリティスキャンが自動実行されていること
- 暗号化が適用されていること（ストレージ・転送）

---

## Step 5: 高度なオブザーバビリティ - 分散トレーシング

### 目的
マイクロサービス間のリクエストフローをトレースし、パフォーマンスボトルネックを特定する

### 作業内容
1. `requirements.txt` / `setup.py` に `opentelemetry-api`, `opentelemetry-sdk`, `opentelemetry-instrumentation-fastapi`, `opentelemetry-exporter-jaeger` 追加
2. `manual_processor/src/observability/tracing.py` 新規作成：
   - OpenTelemetry の初期化
   - トレーサープロバイダー設定（Jaeger または OTLP エクスポーター）
   - 自動インスツルメンテーション設定
   - カスタムスパン作成ヘルパー
3. 自動インスツルメンテーション対象：
   - FastAPI アプリケーション（リクエスト/レスポンス）
   - Celery タスク（タスク開始/終了）
   - Redis クライアント（コマンド実行）
   - HTTP クライアント（外部API呼び出し）
   - データベースクライアント（該当する場合）
4. カスタムスパンの追加：
   - OCR処理の各ステップ
   - AI処理の各ステップ
   - ファイル生成の各ステップ
5. バガゲージ（トレースIDをログに出力）：
   - 構造化ログに `trace_id`, `span_id` フィールド追加
   - ログとトレースの相互参照を可能にする
6. エクスポーター設定：
   - 開発: Jaeger（ローカルまたは Docker）
   - 本番: OTLP エクスポーター（収集器へ）

### 確認方法
- リクエスト実行 → Jaeger UI でトレース閲覧可能
- サービス間の呼び出しがトレースに表示されること
- エラー時に関連するスパンが特定できること
- ログにトレースIDが含まれていること

---

## Step 6: リアルタイムアラートと異常検知

### 目的
システムの異常を早期検知し、適切な通知を行う

### 作業内容
1. `manual_processor/src/observability/alerting.py` 新規作成：
   - アラートルールエンジン実装
   - 複数の通知チャネル対応（Email, Slack, PagerDuty, Webhook）
2. アラートルール定義（`manual_processor/config/alerting_rules.yaml` または Config から）：
   - エラーレート: `5xx error rate > 5% for 5min` → Critical
   - レイテンシ増加: `API p95 latency > 2s for 10min` → Warning
   - リソース逼迫: `Redis memory usage > 80% for 15min` → Warning
   - キュー詰まり: `Celery queue length > 100 for 5min` → Critical
   - 外部API失敗: `Gemini API failure rate > 10% for 5min` → Warning
   - ビジネス指標: `Failed PDF processing rate > 2% for 15min` → Warning
3. 通知チャネル実装：
   - Email: SMTP 連携
   - Slack: Webhook 連携
   - PagerDuty: Events API 連携
   - Webhook: 汎用 HTTP POST
4. サイレンスと抑制機能：
   - メンテナンスウィンドウ中のアラート抑制
   - 同一障害の重複通知防止（間隔を開ける）
5. ダッシュボード連携：
   - Grafana にアラート状況パネル追加
   - アラート履歴の表示

### 確認方法
- 故障シミュレーション → 適切なアラートが飛ぶこと
- 正常復旧 → 復旧アラート（オプション）が飛ぶこと
- 重複アラートが抑制されること
- 各通知チャネルでメッセージが届くこと

---

## Step 7: セキュリティインシデント対応とフォレンジック準備

### 目的
セキュリティインシデント発生時に迅速に対応し、証拠を保全する

### 作業内容
1. `docs/INCIDENT_RESPONSE_PLAYBOOK.md` 作成：
   - 準備段階: ツール、連絡先、権限
   - 識別段階: 兆候の見つけ方、初期対応
   - 制御段階: 拡散防止、証拠保全
   - 撲滅段階: 脅威の除去、システム修復
   - 復旧段階: システム復帰、監視強化
   - 教訓段階: レポート作成、改善策
2. フォレンジックデータ収集スクリプト作成：
   - `scripts/collect-forensics.sh`：
     - システムログ収集
     - アプリケーションログ収集
     - ネットワーク接続情報
     - プロセスリスト
     - ファイルシステムスナップショット（可能な範囲）
     - メモリダンプ（オプション、専門ツール必要）
   - 暗号化・ハッシュで証拠保全
   - タイムスタンプ付きマニフェスト生成
3. 自動証拠保全のトリガー：
   - 特定のアラートレベルでフォレンジック収集自動起動
   - 管理者による手動トリガーも可能
4. 法務・広報対応のテンプレート：
   - `templates/INCIDENT_NOTICE.md`：利用者向け通知
   - `templates/REGULATOR_NOTICE.md`：規制当局向け報告

### 確認方法
- プレイブックが完成していること
- フォレンジックスクリプトが実行可能であること
- 証拠収集プロセスが文書化されていること

---

## Step 8: バックアップとディザスタリカバリ

### 目的
データの損失やシステム障害に備え、迅速に復旧できる体制を整備する

### 作業内容
1. バックアップ戦略の定義：
   - RDB（設定・メタデータ）: 日次フル + 時刻ごとの増分
   - オブジェクトストレージ（アップロード・結果ファイル）: ライフサイクルルールで別リージョン/クラスへ移行
   - Redis: RDB/AOF の定期バックアップ
   - アプリケーションコードと設定: リポジトリから復旧可能
2. バックアップスクリプト作成：
   - `scripts/backup-rds.sh`：PostgreSQL/MySQL 用
   - `scripts/backup-redis.sh`：Redis BGSAVE またはエクスポート
   - `scripts/backup-r2.sh`：rclone または AWS CLI で別バケットへコピー
   - `scripts/backup-all.sh`：全コンポーネントのバックアップ orchestrator
3. 復旧手順の文書化：
   - `docs/DISASTER_RECOVERY.md`：ステップバイステップ復旧ガイド
   - シナリオ別手順: 単一コンポーネント障害、複数コンポーネント障害、地域障害
   - RTO（復旧時間目標）と RPO（復旧ポイント目標）の定義
4. バックアップ検証：
   - 週次でリストアテスト自動実行（ステージング環境へ）
   - リストア後のスモークテスト実行
   - 結果をレポート出力
5. アーカイブポリシー：
   - 法定保存期間が必要なデータの長期アーカイブ（Glacier 等）
   - アーカイブ・リストア手順の文書化

### 確認方法
- バックアップスクリプトが正常に実行されること
- リストアテストが成功すること
- 災害シナリオ想定での復旧手順が明確であること
- RTO/RPO がビジネス要件を満たしていること

---

## Step 9: パフォーマンスベースラインと最適化ガイド

### 目的
システムのパフォーマンス特性を測定し、最適化のためのベースラインを確立する

### 作業内容
1. ベンチマークスイートの標準化：
   - `benchmark/` ディレクトリを整備
   - 標準的なテストデータセット作成（サイズ・複雑度別のPDFサンプル）
   - シナリオ定義：
     - ライトユーザー: 1ページPDF、テキスト中心
     - ミディアムユーザー: 10ページPDF、図表あり
     - ヘビーユーザー: 100ページPDF、手書き複雑
     - バースト: 短時間に大量リクエスト
2. 自動ベンチライン取得スクリプト：
   - `scripts/benchmark-baseline.sh`：
     - フレッシュな環境にデプロイ
     - 各シナリオで負荷テスト実行
     - メトリクス収集（レイテンシ、スループット、エラー率、リソース使用率）
     - 結果をベースラインとして保存
3. パフォーマンス回帰テスト：
   - CI/CD パイプラインにベンチマークステージ追加
   - 前回ベースラインと比較して著しい劣化があれば警告
   - 大幅な改善があればベースライン更新候補
4. 最適化ガイドの作成：
   - `docs/PERFORMANCE_TUNING.md`：
     - ボトルネック特定手順（プロファイリング、トレース分析）
     - 各コンポーネント別最適化ポイント
     - キャッシュ戦略
     - プールサイズ調整
     - データベースインデックス最適化
5. 定期的なパフォーマンスレビュー：
   - 月次でベンチマーク実行とトレンド分析
   - 結果をレポート共有

### 確認方法
- ベンチマークスイートが実行可能であること
- ベースラインが取得できていること
- 回帰テストが機能していること
- 最適化ガイドが実践的であること

---

## Step 10: 法規制対応と業界標準適合

### 目的
特定の業界や地域の規制に対応し、必要な認証取得のための準備を行う

### 作業内容
1. 対象規制の調査とギャップ分析：
   - 医療: HIPAA (米国) または 医療法 (日本)
   - 金融: GDPR または 金融庁ガイドライン
   - 公共機関: セキュリティ基準 (例: ISMAP in Japan)
   - 各規制に必要なコントロールをマトリクス化
2. 必要な追加コントロールの実装：
   - HIPAA 対応（仮に必要な場合）：
     - BAAs（Business Associate Agreement）の基盤
     - 最小必要原則の徹底
     - アクセスログの長期保存（6年）
   - 金融業界対応：
     - より厳格な暗号化基準（FIPS 140-2 準拠モジュール使用）
     - 取引記録の改ざん防止
     - 消去不能ログ（WORM ストレージ）検討
3. 認証取得準備：
   - 必要なドキュメントの整備（セキュリティポリシー、手順書、トレーニング記録）
   - 証拠の収集と保存体制の構築
   - 第三者監査への対応準備
4. 地域別対応オプション：
   - データ居所要件（例: EU データは EU 内に保存）
   - 言語要件（日本語対応は既に実施済み）
   - アクセシビリティ要件（WCAG 2.1 AA 相当）

### 確認方法
- 規制ごとのギャップ分析書が完成していること
- 必要な追加コントロールが実装されていること
- 認証取得のための準備が整っていること
- 地域別オプションが検討されていること

---

## Step 11: エンドユーザードキュメントとトレーニング資料

### 目的
エンドユーザーがシステムを安全かつ効果的に使用できるようにドキュメントとトレーニング資料を整備する

### 作業内容
1. ユーザーマニュアルの作成：
   - `docs/USER_GUIDE.md`：基本操作方法
   - `docs/ADMIN_GUIDE.md`：管理者向け設定・運用ガイド
   - `docs/API_REFERENCE.md`：API詳細リファレンス（自動生成も活用）
   - `docs/SECURITY_GUIDE.md`：セキュリティベストプラクティス
   - `docs/GDPR_GUIDE.md`：GDPR権利の行使方法
2. チュートリアル動画の制作（オプション）：
   - 5分程度のクイックスタートガイド
   - 機能別ハウツービデオ（アップロード→処理→ダウンロード）
   - 管理者タスクビデオ（ユーザー管理、バックアップ、監視）
3. FAQ と トラブルシューティングガイド：
   - `docs/FAQ.md`：よくある質問
   - `docs/TROUBLESHOOTING.md`：Step 12で作成したものをユーザー向けに調整
   - エラーコード別対処法
4. トレーニング環境の提供：
   - `scripts/setup-training-env.sh`：デモデータ付き環境構築
   - `scripts/reset-training-env.sh`：環境リセット
5. 多言語対応の確認：
   - すべてのドキュメントが日本語で利用可能であること
   - 必要に応じて英語版も提供

### 確認方法
- ユーザーガイドが完成していること
- 管理者ガイドが完成していること
- FAQ が網羅的であること
- トレーニング環境がセットアップ可能であること

---

## Step 12: 最終リリース準備とローンチチェックリスト

### 目的
すべての準備が整い、商用リリースが可能な状態を確認する

### 作業内容
1. リリースチェックリストの作成：
   - `docs/RELEASE_CHECKLIST.md`：
     - コード品質: テストカバレッジ ≥ 80%, lint パス, セキュリティスキャン パス
     - ドキュメント: すべての必須ドキュメントが完成・レビュー済み
     - 監視・アラート: すべての重要メトリクスが収集・アラート設定済み
     - バックアップ・リカバリ: バックアップが正常に取れており、リストアテスト済み
     - セキュリティ: 脆弱性が重大なものがない、アクセス制御が適切
     - コンプライアンス: GDPR対応完了、必要な規制に対応
     - パフォーマンス: ベースラインが取得済み、許容範囲内
     - ドキュメントとサポート: ユーザードキュメント完了、サポート体制準備
2. リリース前最終確認ミーティングの実施：
   - 各ステップの担当者が報告
   - 未完了項目の洗い出しと対応計画
   - Go/No-Go の決定
3. リリース手順の文書化：
   - `docs/RELEASE_PROCEDURES.md`：
     - ステージング環境での最終確認
     - 本番環境へのデプロイ手順
     - デプロイ後のスモークテスト手順
     - モニタリング強化期間の設定（最初の24-48時間）
     - ロールバック手順の確認
4. リリースノートの作成：
   - `RELEASE_NOTES.md` の更新
   - 新機能、改善点、修正点、既知の問題を明記
   - アップグレード手順（該当する場合）
5. リリース後のフォローアップ計画：
   - 1週間後のレビューミーティング
   - 1ヶ月後のパフォーマンスレビュー
   - 必要に応じたホットフィックス計画

### 確認方法
- リリースチェックリストのすべての項目が完了していること
- 最終確認ミーティングで Go の決定が出ていること
- リリース手順が文書化されていること
- リリースノートが作成されていること

---

## 依存関係マッピング

```
Step 1 (GDPR エクスポート)    → Step 2
Step 2 (GDPR 削除)              → Step 1, 3
Step 3 (データ最小化)           → Step 1, 2
Step 4 (セキュリティ基準)       → Step 5, 6, 7, 8
Step 5 (分散トレーシング)       → Step 6
Step 6 (アラートと異常検知)     → Step 5, 7, 8
Step 7 (インシデント対応)       → Step 8
Step 8 (バックアップとDR)       → Step 6, 7
Step 9 (パフォーマンスベースライン) → Step 5, 6
Step 10 (法規制対応)            → Step 1-4
Step 11 (ユーザードキュメント)  → 全Step
Step 12 (リリース準備)          → 全Step
```

## 推奨実施順序

| 週 | Steps | 備考 |
|----|-------|------|
| 1前半 | 1, 2, 3 | GDPR 基盤機能実装 |
| 1後半 | 4, 5, 6 | セキュリティ基準 → トレーシング → アラート |
| 2前半 | 7, 8, 9 | インシデント対応 → バックアップ → パフォーマンス |
| 2後半 | 10, 11, 12 | 法規制 → ユーザードキュメント → リリース準備 |

---

*Phase 1-3が完了していることを前提とする。GDPR対応は法的要件により優先度高い。*